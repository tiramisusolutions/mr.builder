Todo
----

DOCUMENTATION

Docs:

- update and change README


Vagrantfile:

- add sync option for /src

Install:

- adjust plone install path
- adjust default password
- add /src folder for sync

VirtualBox:

- add supervisor
- start plone on boot
- give ci[dialog] option to disable that
- sane server-name
- use plone.app_get.ip for getting ip
- create nice docs for how to get ip vi virtualbox thingies

General:

- add circle ci for testing
- add to Atlas
- define upload/store space, maybe on dist.plone.org ?
- cleanup the whole repo, remove ruby, replace with shell or python build scripts
